handoff-archive/ — old HANDOFF.md Change Log entries, moved out per HANDOFF.md's own
archive rule once the live Change Log exceeds ~150 lines. One file per quarter
(YYYY-Q#.md). Each file is self-describing (own header explaining what it is and when
it was cut). Check HANDOFF.md's Current State and live Change Log first — this folder
is history, not the source of truth for what's happening now.
