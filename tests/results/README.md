Tests for `app/(assessment)/results/page.tsx` and its Phase 7 presentational
components (`app/charts/*`, `app/components/RiskCallout.tsx`) that have real branching
logic worth covering directly, as opposed to pure number-layout components. Run via
`npm test` (vitest).
