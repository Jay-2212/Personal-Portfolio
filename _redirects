/index.html / 301
/LSS_Certificate /resume/LSS_Yellow_Belt_Jay_Bharti.pdf 301

# Directory URLs without a trailing slash 308 to the slashed form at the edge.
# Keep sitemap locs on the slashed URLs so Google does not see these as "Page with redirect".
