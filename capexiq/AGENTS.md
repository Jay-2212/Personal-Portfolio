# AGENTS.md

Start with `INTRODUCTION.md`. Then read `HANDOFF.md` for live state and
`DIRECTORY.md` to locate the owner for your task; use `SPEC.md` for product intent.

Keep project detail in the document that owns it. Update `HANDOFF.md` after material
work; do not add duplicate project history or maps here.
